plugins {
    id 'java-library'
}

dependencies {
    compileOnly group: 'com.querydsl', name: 'querydsl-apt', version: "${queryDslVersion}", classifier: 'general'
    compileOnly group: 'org.springframework.boot', name: 'spring-boot-starter-data-mongodb'
}
